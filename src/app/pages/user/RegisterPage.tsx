import {RegisterForm} from "../../features";

// TODO: add back to home, login buttons
export const RegisterPage = () => {
  return (
    <div>
      <h1>Register Page</h1>
      <RegisterForm />
    </div>
  );
};
